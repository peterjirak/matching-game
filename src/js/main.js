'use strict';

const onLoadHandler = () => {
    const appContainer = document.getElementById('app-container');
    const clientWidth = appContainer.clientWidth;
    const clientHeight = appContainer.clientHeight;
}